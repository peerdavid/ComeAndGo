# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                          @controllers.TimeTrackController.index

# Login
GET     /login                                     @controllers.AuthenticationController.loginForm
POST    /login                                     @org.pac4j.play.CallbackController.callback
GET     /logout                                    @org.pac4j.play.ApplicationLogoutController.logout
POST    /logout                                    @org.pac4j.play.ApplicationLogoutController.logout
GET     /signup                                    @controllers.AuthenticationController.signUp
POST    /signup                                    @controllers.AuthenticationController.doSignUp

GET     /edituser                                  @controllers.UserManagementController.indexEditUser
POST    /edituser                                  @controllers.UserManagementController.editUser
GET     /edittimetracks/:userId                    @controllers.TimeTrackController.editTimeTracks(userId : Int, from : java.lang.String ?= null, to : java.lang.String ?= null)
POST    /edittimetracks/:userId                    @controllers.TimeTrackController.saveTimeTrack(userId : Int, from : java.lang.String ?= null, to : java.lang.String ?= null)
POST    /edittimetracks/:userId/addtimetrack       @controllers.TimeTrackController.addTimeTrack(userId : Int, from : java.lang.String ?= null, to : java.lang.String ?= null)
POST    /edittimetracks/:userId/deletebreak/       @controllers.TimeTrackController.deleteBreak(breakid: Int, timetrackId: Int, userId : Int, from : java.lang.String ?= null, to : java.lang.String ?= null)
POST    /edittimetracks/:userId/addbreak/          @controllers.TimeTrackController.addBreak(timetrackId: Int, userId : Int, from : java.lang.String ?= null, to : java.lang.String ?= null)

GET     /deleteUser                                @controllers.UserManagementController.deleteUser(username : String ?= null)

GET     /notification                              @controllers.NotificationController.index
GET     /notification/accept                       @controllers.NotificationController.acceptNotification(notId : Int)
GET     /notification/reject                       @controllers.NotificationController.rejectNotification(notId:Int)

# Come & Go
GET     /come                                      @controllers.TimeTrackController.come
GET     /go                                        @controllers.TimeTrackController.go
GET     /pause                                     @controllers.TimeTrackController.pause

# Map static resources _from the /public folder _to the /assets URL path
GET     /assets/*file                              controllers.Assets.at(path="/public", file)

